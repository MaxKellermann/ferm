
chain input proto tcp mod (limit state) limit 10/s state (INVALID,NEW) REJECT;

chain input mod state {
	mod limit ACCEPT;
	proto udp ACCEPT;
	proto tcp {
		mod state ACCEPT;
	}
	mod (state limit) ACCEPT;
}
