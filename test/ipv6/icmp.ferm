# ferm tries to allow you to use ipv4 keywords for the ipv6 domain.
domain ip6 chain INPUT {
    proto icmp icmpv6-type router-solicitation ACCEPT;
    proto icmp icmp-type neighbour-advertisement DROP;
}
